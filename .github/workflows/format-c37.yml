name: Format C37
on:
  push:
    branches:
      - sprint-c37-retention-engine
permissions:
  contents: write
jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: sprint-c37-retention-engine
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prettier --write src/server/repositories/admin-ur-play-retention-repository.ts src/server/services/admin-ur-play-retention-service.ts src/server/services/admin-ur-play-retention-service.test.ts
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/server/repositories/admin-ur-play-retention-repository.ts src/server/services/admin-ur-play-retention-service.ts src/server/services/admin-ur-play-retention-service.test.ts
          git commit -m "chore: format C37 retention sources" || exit 0
          git push
